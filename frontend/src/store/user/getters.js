export function getEmail(state) {
  return state.email;
}

export function getJwt(state) {
  return state.jwt;
}

export function getName(state) {
  return state.name;
}

export function getId(state) {
  return state.id;
}

export function getUserInfo(state) {
  return state.userInfo;
}

export function getUserNotifs(state) {
  return state.userNotifs;
}

export function getLogged(state) {
  return state.logged;
}
